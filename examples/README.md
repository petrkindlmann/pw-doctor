# Examples

Hands-on walkthroughs you can run against the published `pw-doctor` package.

| Example | What it shows |
|---|---|
| [`heal-walkthrough/`](./heal-walkthrough) | A simple Playwright test broken by a redesigned UI, and the diff `pw-doctor heal` would produce |

Each example is self-contained: clone the repo, `cd` in, follow its README.
